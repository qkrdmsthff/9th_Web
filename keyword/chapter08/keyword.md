- **`Debounce`** 구글링 후 개념 정리 및 코드 작성해보기 🍠
    
    ### 📚 참고자료
    
    [Debounce vs Throttle: Definitive Visual Guide](https://kettanaito.com/blog/debounce-vs-throttle)
    
    ---
    
    - **`Debounce`** 개념 정리 🍠
        
        **디바운스 (Debounce)** 는 **연이어 호출되는 함수들 중 가장 마지막 / 첫 번째 함수만 정해진 시간 이후에 실행되도록 하는 기술**입니다.
        
        - 예시 (엘레베이터의 ‘열림’ 버튼)
            - 누군가 열림 버튼을 누르면 문이 닫히는 타이머가 리셋
            - 사람들이 계속 타면서 버튼을 누르면 문은 닫히지 않음
            - 마지막 사람이 타고 버튼을 누른 뒤 일정 시간 동안 아무도 버튼을 누르지 않으면 문이 닫힘 (함수 실행)
        
        이처럼, 디바운스는 이벤트가 발생하는 것을 잠시 기다렸다가, 마지막 이벤트 발생 후 일정 시간이 지나면 딱 한 번만 실행하도록 하는 **성능 최적화 기법**입니다.
        
        디바운스가 필요한 이유는?
        
        - React 는 상태가 변경되면 렌더링이 다시 일어나는 구조입니다. 만약 사용자의 입력에 따라 즉각적으로 상태를 변경하고, 그 상태가 API 호출이나 무거운 계산을 유발한다면 심각한 성능 저하가 발생할 수 있습니다.
        
        주요 사용 사례
        
        - **검색창 자동완성** : 사용자가 ‘ㅊ’, ‘치’, ‘칯’, ‘치치’ 를 입력할 때마다 API 를 5번 호출하는 것은 서버와 클라이언트 모두 낭비하는 것입니다. 사용자가 타이핑을 멈췄을 때 (예 : 300ms 후) ‘치치’ 라는 키워드로 API 를 한 번만 호출하는 것이 효율적입니다.
        - **윈도우 리사이즈** : 브라우저 창 크기를 조절할 때 수십, 수백 번의 `resize` 이벤트가 발생합니다. 이때마다 레이아웃을 다시 계산하면 버벅임이 발생하기 때문에 리사이즈가 완전히 끝난 후 한 번만 레이아웃을 계산하는 것이 좋습니다.
        - **폼 유효성 검사** : 아이디 중복 확인이나 비밀번호 유효성 검사를 키보드를 누를 때마다 실행하면 불필요한 요청이 많아집니다. 사용자가 입력을 멈췄을 때 검사하는 것이 좋습니다.
            
            
    - **`Debounce`** 코드 작성 🍠
        
        **디바운스 구현 방법**
        
        - React 에서 디바운스를 구현하는 가장 현대적이고 권장되는 방법은 **Custom Hook** 을 만드는 것입니다.
        
        **방법 1 : Custom Hook (useDebounce) 직접 만들기**
        
        ```tsx
        // value : 디바운스 할 대상 (ex. 검색어)
        // delay : 디바운스 시간 (ms 단위)
        function useDebounce (value, delay) => {
        	// 디바운스 된 값을 저장할 state  
        	const [debounceValue, setDebounceValue] = useState(value);
        	
        	useEffect(() => {
        		// delay 이후에 debounceValue 를 value 로 업데이트 하는 타이머 설정
        		const handler = setTimeout(() => {
        			setDebounceValue(value);
        		}, delay);
        		
        		// 핵심
        		/* 
        		value 나 delay 가 변경이 될 때마다 (사용자가 타이핑을 계속 할 때마다)
        		기존의 타이머 (handler) 를 취소하고 (clearTimeout) 하고
        		새로운 타이머를 설정하는 코드
        		*/
        		return () => {
        			clearTimeout(handler);
        		}
        	}, [value, delay]) // value, delay 가 변경될 때만 이 effect 재실행
        	
        	// 최종 debounceValue 값을 반환합니다.
        	return debounceValue;
        }
        ```
        
        ```tsx
        // 위에서 만든 커스텀 훅 사용 예제 (검색창)
        
        const SearchComponent() {
        	// 사용자의 실제 입력 값 (즉시 변경)
        	const [searchTerm, setSearchTerm] = useState('');
        	// 디바운스 된 값 (500ms 의 딜레이 적용)
        	const debounceSearchTerm = useDebounce (searchTerm, 500);
        	
        	// API 호출
        	// 해당 useEffect 는 'debounceSearchTerm' 이 변경될 때만 실행
        	useEffect(() => {
        		if (debounceSearchTerm) {
        			fetch(`/api/search?q=${debounceSearchTerm}`);
        		}
        	}, [debounceSearchTerm])
        	
        	// 사용자가 입력 시 searchTerm 이 즉시 변경되어 input 창은 바로 반응
        	return (
        		<div>
        			<input> 
        			type = "text"
        			placeholder = "검색어 입력"
        			value = {searchTerm}
        			onChange = {(e) => setSearchTerm(e.target.valur)}
        			</input>
        			
        			<p> 실제 입력 값 : {searchTerm} </p>
        			<p> 디바운스 된 값 : {debounceSearchTerm} </p>
        		</div>
        	)
        }
        ```
        
        - 동작 방식
            1. 사용자가 “치치” 를 입력하면 `searchTerm` 은 ㅊ,치,칯,치치 로 즉시 변경
            2. `useDebounce` 훅은 `searchTerm` 이 변경될 때마다 500ms 타이머 설정
            3. 사용자가 500ms 이내에 다음 글자 입력 시 `useEffect` 의 **cleanup 함수**가 실행되어 이전 타이머가 취소되고 새 타이머 설정
            4. 사용자가 “치치” 를 입력하고 500ms 동안 타이핑을 멈추면 마지막으로 설정된 타이머가 실행되어 `debounceSearchTerm` 이 “치치” 로 변경
            5. `debounceSearchTerm` 이 변경되었으므로 `SearchComponent` 의 `useEffect` 가 실행되어 API 호출이 한 번만 일어납니다.
        
        **방법 2 : 라이브러리 (Lodash) 활용**
        
        - 가장 유명한 유틸리티 라이브러리인 **Lodash** 의 `debounce` 함수를 사용하는 것
        - 주의사항 : React 컴포넌트 내에서 Lodash 의 `debounce` 를 잘못 사용하면 렌더링마다 새로운 디바운스 함수가 생성되어 제대로 동작하지 않습니다. **반드시 `useMemo` 또는 `useCallback` 으로 감싸서 함수를 메모이제이션 (memoization)** 해야 합니다.
        
        ```tsx
        import { debounce } from 'lodash';
        
        const SearchComponent () => {
        	const [searchTerm, setSerchTerm] = useState('');
        	
        	// 디바운스 될 실제 로직 (API 호출)
        	const callAPI = (query) => {
        		fetch(`/api/search?q=${query}`);
        	}
        	
        	// useMemo 를 이용하여 디바운스 함수를 메모이제이션
        	// 500ms 딜레이를 가진 디바운스된 함수 생성
        	// 이 함수는 컴포넌트가 리렌더링 되어도 재생성되지 않음
        	const debounceAPICall = useMemo(() => {
        		debounce(callAPI, 500), [] 
        		// 의존성 배열이 비어있으므로 컴포넌트 마운트 시 1번만 생성
        	})
        	
        	// input 변경 핸들러
        	const handleChange = (e) => {
        		const newSearchTerm = e.target.value;
        		
        		setSerchTerm(newSearchTerm);
        		
        		// 메모이제이션 된 디바운스 함수 호출
        		debounceAPICall(newSearchTerm);
        	}
        	
        	return (
        		<input> 
        		type = "text"
        		placeholder = "검색어 입력"
        		value = {searchTerm}
        		onChange = {handleChange}
        		</input>
        	)
        }
        ```
        
- **`Throttling`** 구글링 후 개념 정리 및 코드 작성해보기 🍠
    
    ### 📚 참고자료
    
    [Debounce vs Throttle: Definitive Visual Guide](https://kettanaito.com/blog/debounce-vs-throttle)
    
    ---
    
    - **`Throttling`** 개념 정리 🍠
        
        **쓰로틀 (Throttle) 은 정해진 주기마다 함수가 최대 한 번만 실행되도록 하는 기술**입니다.
        
        - 예시 (게임의 연사 기능)
            - 1초에 10발만 발사할 수 있는 기관총이 있다고 가정
            - 사용자가 1초 동안 마우스를 20번 클릭해도 총알은 1초에 10번만 나감
            - 즉, 아무리 이벤트가 발생해도 **일정한 주기마다 딱 한 번만 함수를 실행**
        
        이처럼 쓰로틀은 지속적인 이벤트를 일정한 시간 간격으로 조절하여 실행하는 **성능 최적화 기법**입니다.
        
        쓰로틀이 필요한 이유
        
        - 디바운스가 마지막 이벤트에 관심이 있었다면, 쓰로틀은 지속적인 이벤트 처리에 관심이 있습니다.
        - React 에서 `scroll`, `mousemove`, `resize` 같은 이벤트는 1초에 수십 - 수백 번씩 매우 짧은 간격으로 발생하기 때문에 해당 이벤트마다 `setState` 를 호출하여 렌더링을 다시 실행하면, UI 가 버벅이고 성능이 심각하게 저하됩니다.
        
        주요 사용 사례
        
        - **무한 스크롤** : 사용자가 스크롤 할 때마다 페이지 끝에 도달하였는지 검사하는데, 이때 1px 씩 스크롤 할 때마다 검사하는 것은 낭비입니다. 200ms 마다 한 번 씩만 위치를 검사하는 것이 효율적입니다.
        - **스크롤 위치에 따른 애니메이션** : 스크롤 위치를 추적하여 특정 UI 를 보여줄 때 사용합니다. 스크롤 하는 동안 주기적으로 UI 가 업데이트 되어야 합니다.
        - **마우스 움직임 추적** : 사용자의 마우스 커서 위치를 실시간으로 추적하는 기능을 구현할 때, 16ms 마다 한 번 씩만 위치를 가져와도 충분히 부드럽습니다.
    - **`Throttling`** 코드 작성 🍠
        
        쓰로틀은 디바운스와 달리 “값” 자체보다는 이벤트 핸들러를 감싸는 방식으로 구현하는 것이 일반적입니다.
        
        **방법 1 : useThrottle 커스텀 훅 사용하기**
        
        ```tsx
        // func : 쓰로틀 할 콜백 함수
        // delay : 쓰로틀 시간 (ms)
        function useThrottle (func, delay) => {
        	// 마지막으로 실행된 시간과 타이머 ID 를 저장하기 위해 ref 사용
        	const lastRunTime = useRef(Date.now());
        	const timerId = useRef(null);
        	
        	const throttledFunc = useCallback((...args) => {
        		const now = Date.now();
        		// 마지막 실행 후 얼마나 시간이 지났는지 계산
        		const timeSinceLastRun = now - lastRunTime.current;
        		
        		// 1. 타이머가 없음 (실행 대기중이 아님)
        		if (!timerId.current) {
        			// 2. 딜레이가 지났다면, 즉시 실행 (Leading Edge)
        			if (timeSinceLastRun >= delay) {
        				lastRunTime.current = now;
        				func(...args);
        			}
        			
        			// 3. 딜레이가 지나지 않았다면, 딜레이가 끝난 후에 실행 (Trailing Edge)
        			else {
        				timerId.current = setTimeout(() => {
        					lastRunTime.current = Date.now();
        					timerId.current = null;
        					func(...args);
        				}, delay - timeSinceLastRun); // 남은 시간 만큼 기다림
        			}
        		}
        		
        		// 이미 타이머 실행 중이라면 이번 호출은 무시
        	}, [func, delay]);
        	
        	// 컴포넌트 언마운트 시 타이머 정리
        	useEffect(() => () => {
        		if (timerId.current) {
        			clearTimeout(timerId.current);
        		}
        	}, []);
        	
        	return throttledFunc;
        }
        ```
        
        ```tsx
        // useThrottle 훅 사용 예제
        const ScrollComponent = () => {
        	const handleScroll = () => {
        		console.log('스크롤 이벤트');
        	}
        	
        	// 커스텀 훅으로 쓰로틀 함수 생성
        	const throttledScrollHandler = useThrottle(handleScroll, 200);
        	
        	useEffect(() => {
        		// 생성된 함수를 리스너로 등록
        		window.addEventListener('scroll', throttledScrollHandler);
        		
        		return () => {
        			window.removeEventListener('scroll', throttledScrollHandler);
        		}
        	}, [throttledScrollHandler]);
        	
        	return (
        		<div>
        			<p> 커스텀 훅으로 쓰로틀링 </p>
        		</div>
        	)
        }
        ```
        
        **방법 2 : 라이브러리 Lodash 사용하기**
        
        - **Lodash** 의 `throttle` 함수와 `useMemo` / `useCallback` 을 함께 사용하는 것이 가장 안정적이고 간단한 방법입니다.
        
        ```tsx
        import { throttle } from 'lodash';
        
        function ScrollComponent() {
        	// 1. 실행할 실제 로직
        	const handleScroll = () => {
        		console.log('스크롤 이벤트');
        	}
        	
        	// useMemo 를 사용하여 쓰로틀 함수 메모이제이션
        	// 200ms 마다 최대 한 번만 handleScroll 함수가 실행
        	const throttledScrollHandler = useMemo(() => 
        		throttle(handleScroll, 200)
        	, []) // 의존성 배열이 비었으므로 컴포넌트 마운트 시 1 번만 실행
        	
        	// 3. 이벤트 리스너 등록
        	useEffect(() => {
        		window.addEventListener('scroll', throttledScrollHandler);
        		
        		// 컴포넌트 언마운트 시 리스너 제거
        		return () => {
        			window.removeEventListener('scroll', throttledScrollHandler);
        		}
        	}, [throttledScrollHandler]); 
        	// throttledScrollHandler가 변경될 때만 effect 재실행
        	
        	return (
        		<div>
        			<p> 커스텀 훅으로 쓰로틀링 </p>
        		</div>
        	)
        }
        ```